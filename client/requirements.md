## Packages
framer-motion | Page transitions and complex animations
zustand | Simple global state management for the cart

## Notes
- Admin routes require 'x-admin-token' header: 'supersecretadmintoken'
- Cart state is persisted in localStorage via Zustand
- Images are dynamic URLs from the database, but we can use Unsplash for seed/placeholder if needed
